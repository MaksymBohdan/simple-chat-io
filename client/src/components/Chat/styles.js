import styled from 'styled-components';

const ChatWrapper = styled.div`
  width: 100%;
  height: 100%;
  background-color: rgba(0, 0, 0, 0.4);
  overflow: auto;
  position: relative;
`;

export { ChatWrapper };
